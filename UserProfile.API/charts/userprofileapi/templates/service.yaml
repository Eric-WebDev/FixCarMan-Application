apiVersion: v1
kind: Service
metadata:
  name: {{ template "userprofileapi.fullname" . }}
  labels:
    app: {{ template "userprofileapi.name" . }}
    chart: {{ template "userprofileapi.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "userprofileapi.name" . }}
    release: {{ .Release.Name }}
